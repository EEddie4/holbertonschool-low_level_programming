#include "holberton.h"
/**
* cap_string - function
* @s: pointer
*
*Description: function to capitalize all words of a string
* Return: char
*/
char *cap_string(char *s)
{
	int i;

	for (i = 0; s[i]; i++)
		;

	return (s);
}
